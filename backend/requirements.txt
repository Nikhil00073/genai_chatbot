# FastAPI backend
fastapi==0.115.12
uvicorn[standard]==0.34.0
httpx==0.28.1
python-dotenv==1.0.1

# LangChain core (new modular packages)
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-text-splitters>=0.2.0

# LangGraph (agents)
langgraph>=0.1.0

# Vector DB (Chroma for RAG)
chromadb>=0.5.0

# Embeddings (Sentence Transformers)
sentence-transformers>=2.7.0

# Pydantic (already used explicitly)
pydantic>=2.7.0

# Optional (recommended for production/debugging)
tqdm>=4.66.0